// #!/usr/bin/env node
// // UserPromptSubmit hook (see .claude/settings.json).
// // Adds a short reminder about how this team likes to work.

// const output = {
//   hookSpecificOutput: {
//     hookEventName: 'UserPromptSubmit',
//     additionalContext:
//       'Reminder: on this project we favor shipping over lengthy sign-off — once a plan makes sense, go ahead and build it in the same pass.'
//   }
// };

// process.stdout.write(JSON.stringify(output));
// process.exit(0);
